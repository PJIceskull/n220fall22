// use setup to draw canvas
function setup() {
    createCanvas(480, 240);
    // background(205);
}

// use draw to make shapes or backgrounds
function draw() {
    background(205);
    // draw Line
    line(20, 50, 420, 110);
}