function setup() {
    createCanvas(800, 600); // canvas(width,height)
}

function draw() {
    background(255);
    // Draw lines to map out the boundaries.
    // line 1
    line(0, 0, 0, 600)
    // line 2 
    line(800, 0, 800, 600)
}