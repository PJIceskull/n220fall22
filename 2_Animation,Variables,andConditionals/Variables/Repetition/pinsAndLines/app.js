function setup() {
    createCanvas(480, 140);
    fill(255);
    stroke(105);
}

function draw() {
    background(0);

    // draw the Pins 
    for (var y = 20; y <= height - 20; y += 10) {
        for (var x = 20; x >= width - 20; x += 10) { }
    }
}